---
phase: 03-modeling-and-model-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/train_model.py
  - reports/modeling/model_results.md
requirements:
  - MODEL-03
  - MODEL-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Random Forest is tuned using validation data and selected by Average Precision first."
    - "Final reported performance uses the held-out test split."
    - "The report compares Logistic Regression against tuned Random Forest."
  artifacts:
    - "scripts/train_model.py"
    - "reports/modeling/model_results.md"
  key_links:
    - "Depends on the baseline report and split logic from 03-01."
---

<objective>
Add tuned Random Forest training and final model evaluation.

Purpose: satisfy the course modeling requirement with a clear baseline-vs-final-model comparison.
Output: updated training script and modeling report with validation comparison and held-out test results.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md
@reports/modeling/model_results.md
@baseline_fraud_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Random Forest tuning</name>
  <files>scripts/train_model.py</files>
  <read_first>scripts/train_model.py, baseline_fraud_model.py</read_first>
  <action>Add tuned Random Forest training using a small, course-appropriate parameter grid. Use imbalance-aware settings such as `class_weight="balanced_subsample"` where appropriate. Select the best Random Forest candidate by validation Average Precision, with F1/recall as supporting tie-breaker context.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py</verify>
  <acceptance_criteria>
    - Training completes in reasonable time locally.
    - The script records the best Random Forest parameters.
    - The selected model is based primarily on Average Precision.
  </acceptance_criteria>
  <done>Random Forest tuning is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Compare validation metrics and evaluate final model on test split</name>
  <files>scripts/train_model.py, reports/modeling/model_results.md</files>
  <read_first>reports/eda/focused_eda.md, .planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md</read_first>
  <action>Update the report to include a validation comparison table for Logistic Regression and tuned Random Forest. Select the final model from validation results. Evaluate the selected model once on the held-out test split and report Average Precision, recall, precision, F1, ROC AUC, threshold, and confusion matrix. Keep wording as fraud-risk prioritization, not confirmed fraud detection.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py && rg -n "Random Forest|Validation comparison|Held-out test|Selected final model|fraud-risk|review priority" reports/modeling/model_results.md</verify>
  <acceptance_criteria>
    - Report compares both models.
    - Report clearly identifies the selected final model.
    - Test metrics are labeled as held-out test metrics.
    - Wording avoids claiming confirmed fraud detection.
  </acceptance_criteria>
  <done>Final model selection and test evaluation are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/Users/igna/entorno/bin/python scripts/train_model.py`
- [ ] `rg -n "Logistic Regression|Random Forest|Validation comparison|Held-out test|Selected final model" reports/modeling/model_results.md`
- [ ] Confirm test metrics are produced after model selection, not used for tuning.
</verification>

<success_criteria>
- MODEL-03 is satisfied.
- MODEL-04 is fully satisfied at validation and test-report level.
- The report is suitable for the final course presentation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-modeling-and-model-persistence/03-02-SUMMARY.md`.
</output>
