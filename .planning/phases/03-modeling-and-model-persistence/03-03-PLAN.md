---
phase: 03-modeling-and-model-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - scripts/train_model.py
  - reports/modeling/model_results.md
  - models/fraud_risk_model.joblib
  - models/model_metadata.json
requirements:
  - MODEL-06
  - MODEL-07
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The saved joblib model can be loaded without retraining."
    - "Metadata contains features, selected threshold, model parameters, and metrics."
    - "The artifact is ready for Phase 4 Streamlit loading."
  artifacts:
    - "models/fraud_risk_model.joblib"
    - "models/model_metadata.json"
    - "reports/modeling/model_results.md"
  key_links:
    - "Depends on selected final model from 03-02."
---

<objective>
Persist the final model and metadata for the Streamlit app.

Purpose: produce stable model artifacts that Phase 4 can load directly.
Output: `models/fraud_risk_model.joblib`, `models/model_metadata.json`, and report references to both artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md
@scripts/train_model.py
@reports/modeling/model_results.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save final model with joblib</name>
  <files>scripts/train_model.py, models/fraud_risk_model.joblib</files>
  <read_first>scripts/train_model.py</read_first>
  <action>Update the training script to create `models/` and save the selected final trained pipeline to `models/fraud_risk_model.joblib` using joblib. The saved object must include preprocessing and estimator steps needed for prediction from the 12 feature columns.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py && test -s models/fraud_risk_model.joblib</verify>
  <acceptance_criteria>
    - `models/fraud_risk_model.joblib` exists and is non-empty.
    - A short load smoke test with joblib succeeds.
    - The loaded object exposes `predict_proba`.
  </acceptance_criteria>
  <done>Final model is persisted with joblib.</done>
</task>

<task type="auto">
  <name>Task 2: Save metadata and document artifacts</name>
  <files>scripts/train_model.py, models/model_metadata.json, reports/modeling/model_results.md</files>
  <read_first>.planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md, reports/modeling/model_results.md</read_first>
  <action>Write `models/model_metadata.json` with selected model name, feature columns, target definition, threshold, validation metrics, test metrics, best parameters, dataset row count, positive-class rate, random state, generated timestamp, and review-priority disclaimer. Update `reports/modeling/model_results.md` to mention the saved model and metadata paths.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py && /Users/igna/entorno/bin/python -m json.tool models/model_metadata.json >/tmp/model_metadata_valid.json && rg -n "fraud_risk_model.joblib|model_metadata.json|review priority" reports/modeling/model_results.md</verify>
  <acceptance_criteria>
    - Metadata JSON is valid.
    - Metadata includes all 12 feature names in order.
    - Metadata includes threshold and metrics.
    - Report points to both saved artifacts.
  </acceptance_criteria>
  <done>Model artifacts and metadata are ready for Phase 4.</done>
</task>

<task type="auto">
  <name>Task 3: Add model load smoke check</name>
  <files>scripts/train_model.py, reports/modeling/model_results.md</files>
  <read_first>scripts/train_model.py, models/model_metadata.json</read_first>
  <action>Add or document a lightweight smoke check that loads the joblib model and predicts probabilities for a small sample using the 12 features. This can be built into `scripts/train_model.py` after saving or implemented as a small helper function inside that script.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py && /Users/igna/entorno/bin/python - <<'PY'\nfrom pathlib import Path\nimport joblib\nmodel = joblib.load(Path('models/fraud_risk_model.joblib'))\nprint(type(model).__name__)\nprint(hasattr(model, 'predict_proba'))\nPY</verify>
  <acceptance_criteria>
    - Joblib artifact loads successfully.
    - Loaded model supports probability output.
    - The check does not retrain the model when used by Phase 4.
  </acceptance_criteria>
  <done>Persistence is verified by load smoke test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/Users/igna/entorno/bin/python scripts/train_model.py`
- [ ] `test -s models/fraud_risk_model.joblib`
- [ ] `/Users/igna/entorno/bin/python -m json.tool models/model_metadata.json`
- [ ] Load `models/fraud_risk_model.joblib` with joblib and confirm `predict_proba`.
- [ ] `rg -n "fraud_risk_model.joblib|model_metadata.json|review priority" reports/modeling/model_results.md`
</verification>

<success_criteria>
- MODEL-06 and MODEL-07 are satisfied.
- Phase 4 can use saved artifacts without reading the full dataset or retraining.
- Model outputs remain framed as review-priority/fraud-risk probabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/03-modeling-and-model-persistence/03-03-SUMMARY.md`.
</output>
