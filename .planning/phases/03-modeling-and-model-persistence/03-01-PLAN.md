---
phase: 03-modeling-and-model-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train_model.py
  - reports/modeling/model_results.md
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-04
  - MODEL-05
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Training uses only the official 12 v1 features."
    - "The target is derived from AAER_ID and framed as known fraud-related label, not proof of honesty for unlabeled rows."
    - "The Logistic Regression baseline is trained and evaluated."
    - "Accuracy is explicitly explained as misleading for the imbalanced dataset."
  artifacts:
    - "scripts/train_model.py"
    - "reports/modeling/model_results.md"
  key_links:
    - "Training script reuses or supersedes useful logic from baseline_fraud_model.py."
---

<objective>
Create the reproducible modeling script foundation and Logistic Regression baseline.

Purpose: make model training rerunnable from the local dataset and produce a readable metrics report for the baseline model.
Output: `scripts/train_model.py` and initial `reports/modeling/model_results.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md
@docs/data_source.md
@reports/eda/focused_eda.md
@baseline_fraud_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reproducible training script</name>
  <files>scripts/train_model.py</files>
  <read_first>baseline_fraud_model.py, docs/data_source.md, reports/eda/focused_eda.md</read_first>
  <action>Create `scripts/train_model.py`. It should load `data/Cleaned_data_1995_2018.csv`, validate the official 12 feature columns plus `AAER_ID`, convert `Financial_Year` from values like `FY2001` into a numeric year, derive `target_fraud`, and create deterministic stratified train/validation/test splits. Keep constants for paths, features, target, and random state.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py</verify>
  <acceptance_criteria>
    - Script runs far enough to load, validate, split, and print or write dataset summary.
    - It reports `87,974` rows, `12` features, and the rare positive-class ratio.
    - It does not use any columns outside the official 12 features for modeling.
  </acceptance_criteria>
  <done>Modeling script has reliable data loading and splitting foundation.</done>
</task>

<task type="auto">
  <name>Task 2: Train and evaluate Logistic Regression baseline</name>
  <files>scripts/train_model.py, reports/modeling/model_results.md</files>
  <read_first>baseline_fraud_model.py, .planning/phases/03-modeling-and-model-persistence/03-CONTEXT.md</read_first>
  <action>Add a Logistic Regression baseline using a scikit-learn Pipeline with median imputation, scaling, and imbalance-aware configuration. Evaluate on the validation split using Average Precision, recall, precision, F1, ROC AUC, and confusion matrix at the selected threshold. Generate `reports/modeling/model_results.md` with the dataset summary, metric definitions in simple language, baseline metrics, and an explanation of why accuracy is misleading.</action>
  <verify>/Users/igna/entorno/bin/python scripts/train_model.py && rg -n "Logistic Regression|Average Precision|accuracy is misleading|confusion matrix|official 12" reports/modeling/model_results.md</verify>
  <acceptance_criteria>
    - Logistic Regression baseline appears in the report.
    - Report includes Average Precision, recall, precision, F1, ROC AUC, and confusion matrix.
    - Report explains that a model can get high accuracy by predicting almost everything as non-fraud, so accuracy is not the primary metric.
  </acceptance_criteria>
  <done>Baseline model and metric explanation are ready.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/Users/igna/entorno/bin/python scripts/train_model.py`
- [ ] `rg -n "Logistic Regression|Average Precision|recall|precision|F1|ROC AUC|confusion matrix|accuracy is misleading" reports/modeling/model_results.md`
- [ ] Confirm the script only uses the official 12 feature columns for `X`.
</verification>

<success_criteria>
- MODEL-01, MODEL-02, the baseline part of MODEL-04, and MODEL-05 are satisfied.
- The baseline is understandable for a non-technical evaluator.
- The final model is not saved yet; persistence is handled by a later Phase 3 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-modeling-and-model-persistence/03-01-SUMMARY.md`.
</output>
