---
phase: 01-data-source-ingestion-sqlite-and-sql-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/create_database.py
  - data/fraud_financial.db
requirements:
  - SQL-01
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Running the database creation script creates or refreshes data/fraud_financial.db."
    - "SQLite table financial_records contains 87974 rows."
    - "Table financial_records includes AAER_ID and the 12 official feature columns."
  artifacts:
    - "scripts/create_database.py"
    - "data/fraud_financial.db"
  key_links:
    - "The SQLite table is loaded from the validated CSV path documented in Phase 1."
---

<objective>
Create the SQLite database from the local Kaggle CSV.

Purpose: Satisfy the database-storage requirement and provide a structured source for SQL analysis.
Output: `data/fraud_financial.db` with table `financial_records`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-data-source-ingestion-sqlite-and-sql-analysis/01-CONTEXT.md
@scripts/validate_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite creation script</name>
  <files>scripts/create_database.py</files>
  <read_first>scripts/validate_data.py, baseline_fraud_model.py</read_first>
  <action>Create `scripts/create_database.py`. It should read `data/Cleaned_data_1995_2018.csv`, validate the file by reusing or matching `scripts/validate_data.py` expectations, write a SQLite database at `data/fraud_financial.db`, replace table `financial_records`, and create a useful index on `Financial_Year` plus an index on `AAER_ID` if practical. Use standard library `sqlite3` and pandas. Print database path, table name, row count, and column count.</action>
  <verify>/Users/igna/entorno/bin/python scripts/create_database.py</verify>
  <acceptance_criteria>
    - `data/fraud_financial.db` exists after running the script.
    - Script output reports 87974 rows loaded.
    - Script can be run repeatedly and refreshes the table.
  </acceptance_criteria>
  <done>Database can be generated from the CSV with one command.</done>
</task>

<task type="auto">
  <name>Task 2: Verify database row count and schema</name>
  <files>data/fraud_financial.db</files>
  <read_first>scripts/create_database.py</read_first>
  <action>Run a small verification command using Python sqlite3. Confirm `financial_records` exists, has 87974 rows, and includes `AAER_ID`, `Financial_Year`, `sale`, `ni`, `at`, `lt`, `che`, `rect`, `invt`, `cogs`, `txt`, `xint`, `prcc_f`.</action>
  <verify>/Users/igna/entorno/bin/python -c "import sqlite3; con=sqlite3.connect('data/fraud_financial.db'); cols=[r[1] for r in con.execute('PRAGMA table_info(financial_records)')]; print(con.execute('select count(*) from financial_records').fetchone()[0]); print(all(c in cols for c in ['AAER_ID','Financial_Year','sale','ni','at','lt','che','rect','invt','cogs','txt','xint','prcc_f']))"</verify>
  <acceptance_criteria>
    - Verification prints `87974`.
    - Verification prints `True` for required columns.
  </acceptance_criteria>
  <done>Database content is verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/Users/igna/entorno/bin/python scripts/create_database.py`
- [ ] Required table has 87974 rows.
- [ ] Required columns are present.
</verification>

<success_criteria>
- SQL-01 is satisfied.
- Database generation is reproducible.
- No dependency on external services is needed after the CSV exists locally.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-source-ingestion-sqlite-and-sql-analysis/01-02-SUMMARY.md`.
</output>
